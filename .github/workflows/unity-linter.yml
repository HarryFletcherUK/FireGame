name: Unity Lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: dotnet restore
      - name: Run Unity Lint
        run: dotnet run -p src/UnityLint.csproj
